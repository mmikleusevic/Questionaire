@using Web.Pages.Custom
<ConfirmActionModal Modal="@Modal"
                    Entity="@User"
                    Name="@User?.Email"
                    ActionText="delete"
                    Action="Delete"
                    OnEntityChanged="OnUserChanged"/>